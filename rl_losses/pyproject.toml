[project]
name = "rl-losses"
version = "0.1.0"
description = "Policy-gradient losses (PPO clip, GSPO, CISPO, REINFORCE) for prime-rl's custom loss hook."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["rl_losses"]
